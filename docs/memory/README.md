# Memory Docs

This folder contains the deeper memory export layer:
- memory stack v2;
- memory core v1;
- retrieval policy matrix;
- authority priority behavior;
- runtime serving architecture;
- typed memory as a first-class serving plane.

These docs capture the serving and authority model behind a usable memory system, not just generic notes about memory.
